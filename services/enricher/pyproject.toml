[project]
name = "enricher"
version = "0.1.0"
description = "Reading Shelf: URL metadata enrichment service"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.141.1",
    "httpx>=0.28.1",
    "uvicorn[standard]>=0.52.4",
]
