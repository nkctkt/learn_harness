packages:
  - apps/*
  - packages/*

# pnpm 11 は依存の install スクリプトを既定で実行せず、判断を allowBuilds に明記させる(供給網対策)。
# esbuild の postinstall はバイナリ検証のみで、optionalDependencies 経由で動作するため false(実行しない)。
allowBuilds:
  esbuild: false

# 依存は完全固定で記録する(^ / ~ を付けない)。pnpm 11 では .npmrc ではなくここに書く。
saveExact: true
